import SwiftUI

// To run this example in Xcode:
// 1. Open CustomizableSlider.xcodeproj
// 2. Select the "CustomizableSlider" scheme and run on a simulator

@main
struct ExampleApp: App {
    var body: some Scene {
        WindowGroup {
            ContentView()
        }
    }
}
